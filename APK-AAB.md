## خطوات استخراج ملف APK ورفعه على Google Play

### 1️⃣ سحب المشروع محلياً
```bash
# استنساخ المشروع من GitHub
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO

# تثبيت المتطلبات
npm install
```

### 2️⃣ إضافة منصة Android
```bash
# إضافة Android
npx cap add android

# بناء المشروع
npm run build

# مزامنة الملفات
npx cap sync android
```

### 3️⃣ فتح المشروع في Android Studio
```bash
npx cap open android
```

### 4️⃣ إنشاء ملف APK / AAB
في Android Studio:
1. **Build → Generate Signed Bundle / APK**
2. اختر **Android App Bundle (AAB)** لـ Google Play أو **APK** للتثبيت المباشر
3. أنشئ **Keystore** جديد (احفظه في مكان آمن!)
4. أدخل بيانات التوقيع واضغط **Next**
5. اختر **release** ثم **Create**

### 5️⃣ رفع على Google Play
1. افتح [Google Play Console](https://play.google.com/console)
2. أنشئ **تطبيق جديد**
3. املأ بيانات التطبيق (الاسم، الوصف، الأيقونات، لقطات الشاشة)
4. في **Production → Create new release**
5. ارفع ملف **AAB**
6. أكمل متطلبات المتجر (سياسة الخصوصية، التصنيف العمري)
7. اضغط **Submit for review**

### ⚠️ متطلبات مهمة
- حساب مطور Google Play (رسوم $25 مرة واحدة)
- Android Studio مثبت
- Java JDK 11+
- صفحة سياسة خصوصية (مطلوبة)

قراءة دليل النشر الكامل
إنشاء صفحة سياسة الخصوصية
إضافة أيقونات التطبيق

سأتحقق من ملفات المشروع لإجراء أي تحسينات ضرورية قبل السحب.

المشروع جاهز بشكل ممتاز! لكن سأضيف بعض التحسينات الصغيرة للموبايل:
\\n     \\n \\n+     \\n+     \\n+     \\n+ \\n     \\n     \\n     \\n   \\n \\n   \\n     \\n     \\n+     \\n+       // Capacitor initialization check\\n+       if (window.Capacitor) {\\n+         document.body.classList.add('capacitor-app');\\n+       }\\n+     \\n   \\n \\n*** End Patch\\n\"}">

## ✅ المشروع جاهز للسحب!

### نعم، يمكنك استخدام PNPM! 🎉

```bash
# بدلاً من npm install استخدم:
pnpm install

# باقي الأوامر تبقى كما هي:
pnpm run build
npx cap sync android
npx cap open android
```

### الخطوات الكاملة مع PNPM:
```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
pnpm install
npx cap add android
pnpm run build
npx cap sync android
npx cap open android
```

### ✅ تحسينات تمت إضافتها:
- دعم Capacitor في HTML
- كشف تلقائي لبيئة التطبيق الأصلي

إنشاء صفحة سياسة الخصوصية
إضافة شاشة البداية
تجهيز أيقونات التطبيق
